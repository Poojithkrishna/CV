<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed by the Dart VM service / hot reload in debug builds only. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
