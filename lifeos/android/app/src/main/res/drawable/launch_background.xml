<?xml version="1.0" encoding="utf-8"?>
<!-- Shown while Flutter initializes: the same crimson-violet-on-black
     brand gradient as the app icon, with the emblem centered, so the
     cold-start moment already feels like Demon Origin instead of a
     blank flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="315"
                android:startColor="#1B0F2E"
                android:endColor="#0A0610" />
        </shape>
    </item>
    <item android:gravity="center">
        <bitmap
            android:src="@mipmap/ic_launcher_foreground"
            android:gravity="center" />
    </item>
</layer-list>
