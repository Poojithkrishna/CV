<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Sampled from the app icon's own backdrop, so the adaptive icon's
         background layer matches the emblem art exactly. -->
    <color name="ic_launcher_background">#120A1C</color>
</resources>
